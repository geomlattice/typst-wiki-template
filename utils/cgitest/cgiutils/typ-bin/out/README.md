# utils/cgitest/cgiutils/typ-bin/out 

pdf output is dumped here and expected to be scanned by tools such as `pdfgrep`.

For now, assume that binary pdf data should not be source controlled; eg ensure this path for `*.pdf` is in the `.gitignore`
