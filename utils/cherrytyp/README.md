# utils/cherrytyp

Cherrypy is a very old http server and can be used for json web tokens, cookies, and REST API

So far `main.py` demonstrates a REST API and `sessiondemo.py` demonstrates session management with cookies.
